import BreadcrumbInstitutional, {
  Props,
} from "deco-sites/australroupas/components/ui/BreadcrumbInstitutional.tsx";

export default function BreadcrumbInstitutionalSection(props: Props) {
  return <BreadcrumbInstitutional {...props} />;
}
